<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <TaskOutputDir>$(SolutionDir)build\bin\$(Configuration)\$(TargetFramework)\</TaskOutputDir>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Build.Utilities.Core" Version="18.0.2" />
  </ItemGroup>

  <!--<Target Name="CopyDllToBuildFolder" AfterTargets="Build">
    <MakeDir Directories="$(TaskOutputDir)" />
    <Copy SourceFiles="$(OutputPath)$(AssemblyName).dll"
          DestinationFolder="$(TaskOutputDir)" />
  </Target>-->

</Project>
